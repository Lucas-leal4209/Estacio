// Online C compiler to run C program online
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    time_t hora=time(NULL);
    printf("Hora atual: %s", ctime(&hora));

    return 0;
}
