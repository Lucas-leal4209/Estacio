#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

int main() {
    char linhaA[50];
    printf("Alimente a string com caracteres:\n");
    fgets(linhaA,50,stdin);
    printf("%s",linhaA);
    printf("%lu",strlen(linhaA)-1);
    return 0;
}
