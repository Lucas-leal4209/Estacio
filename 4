#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char linha[50];
    printf("Digite a string:\n");
    fgets(linha,50,stdin);
    
    printf("Essa string possui %lu caracteres incluindo espaços e acentos.", strlen(linha)-1);
    return 0;
}
