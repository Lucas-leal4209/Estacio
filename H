// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int vetor[10];
    for(int i=0;i<10;i++){
        vetor[i]=(i + 5 * i) * (i + 1);
        printf("%d ",vetor[i]);
    }
    printf("\n");

    return 0;
}
